# Copy to backend/.env.local (gitignored). Values here override .env — use for local dev only.

NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Use repo data folder (default). Uncomment to use a separate copy of the JSON files:
# DATA_DIR=data
